# REP-0004：定向阅读与配对维护试用

日期：2026-09-08，Asia/Shanghai；实际会话运行时 UTC 仍为 2026-09-07。任务已完成，结果存在对照污染和一项冷读证据缺口，不能据此判断插件的纯增量收益。需求与预注册见 [PLAN-0004](../plans/0004-paired-maintenance-trial.md)，定向阅读见[比较记录](0004-reading-comparison.md)，独立判断见 [REV-0004](../reviews/0004-comparison-review.md)。

## 环境与真实执行

共同基线 `a5f072e5bca0f27c33264ce4562fd08c4ffbbd18`，插件 0.1.0；镜像固定为 `sha256:aa46e31c71577eb37c1e1d856427d9d159ddd5a41aa92472af722838b1c3a159`，Codex CLI 0.149.1、Python 3.11.2、模型 `gpt-5.6-terra`。每个容器 2 CPU / 4 GiB，实施限时 900 秒，冷读限时 300 秒；禁用 memories/multi_agent，读者另禁用 plugins。未挂载个人配置或聊天；认证只从单文件只读挂载复制到容器 tmpfs。网络为 host 以使用现有模型代理，这不是网络隔离验证；任务禁止额外联网研究。

按 A→B、B→A 完成四次独立实施，每份产物紧接一次新容器无插件冷读，共八次会话，全部退出 0，无超时、模型重跑或执行中人工干预。基线、分发包、研究项目 archive、提示、验收器及预注册的字节/摘要值均已归档；执行者未获得本轮方案或验收答案。当前计划的收尾更新与冻结版分别保留，不把更新后的文件冒充原输入。

所有 A 会话实际注册 marketplace、安装 0.1.0，并从安装缓存完整读取 Skill；B 未安装。四位读者未安装插件，A 的唯一实验启用行均已移除，实际产物保留。卸载列表及目标缓存均为空。

## 逐项结果

| 条件 / 样本 | 代码 | 文档与维护 | 冷读 | 实际 Skill 接触 |
| --- | --- | --- | --- | --- |
| A1 `edaf048d` | 独立 13/13；为测试抽取 handoff 条件，整合时不采用该额外改动 | 沿用已有计划及复现，新增审查占位明确 Not Run；保留历史 | 工具输出确认 6 项单测通过，能找到下一步和限制 | 从安装缓存读取完整 Skill |
| B1 `08a618d4` | 独立 13/13；choices + 正整数转换 + 重复检查，推荐整合 | 说明充分；新 REP 未逐文件登记，最终台账已补齐 | 工具输出确认 4 项单测通过，找到验证及审查待办 | 未安装，但搜索输出暴露旧证据中的完整转义 Skill，存在污染 |
| B2 `4ce1f058` | 独立 13/13；手工分支较多 | 新计划和复现有用途；未更新当前计划导航，但读者成功检索到 | 工具输出确认 7 项单测通过，识别未执行审查 | 未安装，但搜索旧 stdout/before JSON 暴露完整转义 Skill，存在污染 |
| A2 `50aa8b68` | 独立 13/13；与 B1 同类实现 | 导航更新有用途；将内存 compile 写成 py_compile，最终说明未采用该错误 | 发出验证命令、退出 0，报告 6 项通过；该事件输出为空，无法独立核实当时测试数量与输出，读者正确指出术语错误 | 从安装缓存读取完整 Skill |

四份均保留预置的未提交 README 注释，插件五个文件未变；未观察到批准需求或历史运行结果被改写。四位读者的源码/文档前后差异均为空。没有按文档数量、路径模板或单测条数评优；独立评审先对匿名产物固定判断，再查看 mapping 与实施轨迹。内容本身可能暴露条件，不承诺完全盲法。

**对照污染是实际观察，不只是可能性。** B1 的 item_3 和 B2 的 item_2 搜索命中基线中旧实验的 before/after/stdout JSON，其中含完整 Skill 文本。因此这里比较的是“安装启用”和“未安装但可被旧证据暴露 Skill”，不能解释为纯净的有/无 Skill 效果。保留四份样本，不剔除或重跑受污染样本。A 的本轮输入及耗时更高也不能直接归因于插件；仅两对样本、任务和模型波动、已有治理及污染均限制推断。

## 时间和用量

| 样本 / 条件 | 会话 | 秒 | 输入 token | 其中缓存输入 | 输出 token |
| --- | --- | ---: | ---: | ---: | ---: |
| A1 `edaf048d` | 实施 | 205.154 | 715152 | 653568 | 9872 |
| A1 `edaf048d` | 无插件冷读 | 57.452 | 147351 | 111360 | 2089 |
| B1 `08a618d4` | 实施 | 169.874 | 577807 | 527104 | 7317 |
| B1 `08a618d4` | 无插件冷读 | 62.067 | 132890 | 98048 | 2318 |
| B2 `4ce1f058` | 实施 | 174.192 | 466993 | 421888 | 8368 |
| B2 `4ce1f058` | 无插件冷读 | 74.997 | 249945 | 209920 | 3057 |
| A2 `50aa8b68` | 实施 | 452.146 | 965623 | 879104 | 10769 |
| A2 `50aa8b68` | 无插件冷读 | 54.122 | 101136 | 69632 | 2061 |

计时包含容器内插件生命周期开销。输入 token 已包含缓存输入；两列不能相加。输出 token 包含模型报告的推理部分，原始 usage 另存细项。上表仅作本次资源记录，不合成质量总分或成本优越性结论。

## 最小修复与验证

依据匿名评审，从 B1 原样选择 [入口脚本](../../tests/run-plugin-smoke.py) 和 [56 行回归测试](../../tests/test_run_plugin_smoke.py)，没有复制样本的实验启用声明、额外治理变更或成套文档。使用内置 choices 拒绝未知/内部 handoff；正整数转换拒绝非正超时；重复检查在任何目录创建、Docker 或认证访问前调用 parser.error。原 handoff 调度条件保持不变。使用说明、设计依据和接续状态整合在本轮既有文档中。

| 验证 | 实际结果 |
| --- | --- |
| 冻结基线的独立验收 | 6/13；7 类非法输入失败，证明原缺陷存在 |
| 四份候选独立验收 | 各 13/13；无效输入无副作用，合法输入与 handoff 使用受控替身 |
| 最终主机 Python 3.14.4 | unittest 4 项 Passed；独立契约 13/13 Passed |
| 最终固定镜像 Python 3.11.2 | 禁网、无认证挂载、只读源码的容器内 unittest 4 项及契约 13/13 Passed |
| 源码、文档、引用、归档 | Passed；最终独立审查无剩余阻断，保留已披露的实验限制 |
| Windows、真实模型 smoke 的入口集成重跑 | Not Run；本轮只对最终入口运行离线回归，八次模型会话由独立实验 runner 执行 |
| 联网检索专项、插件自动委派 reviewer、自身正式接管 | Not Run；不在本轮范围 |

复验命令见 [tests/README.md](../../tests/README.md)。独立验收第一次只恢复目标等三个文件，合法路径因缺少基线 plugins 目录失败；这是主 Agent 的验收准备错误，恢复完整快照后通过。原始失败和修正后的结果同时保存，没有重跑模型。执行会话干预 0、模型重试 0；验收准备纠正 1，分开记录。

A2 的 compile/py_compile 术语错误未进入最终说明；其空测试输出仍保留为冷读证据不足，最终主 Agent 复验不能补造当时读者输出。样本日期保留 UTC 当天原文，主报告注明用户时区，未将日期差异判为实现错误。

## 资料补充、清理与后续

R-22 [argparse](../reference/python-argparse.md) 已补中文摘要、原链接和许可范围。用户在实验开始后追加的 [planning-with-files 插件参考](../reference/planning-with-files-plugin.md) 已归入固定 P-01：包括 manifest、实际 Skill、hooks 与操作测试，独立审查通过；未安装该上游插件、未更新子模块，也未改变冻结实验输入。

本轮模型容器、tmpfs 认证副本和执行工作区已清理，原有容器及镜像保留；最终离线复验容器也自动移除。完整证据保存在 [evidence/0004](evidence/0004/README.md)，原始压缩包包含 146 文件及逐文件 SHA。匿名评审工作副本在审查结束后清理，保留的归档是证据，不是运行工作区。未安装到个人 Codex，未推送或发布；主仓库未添加自身启用声明，插件仍为 0.1.0。

下一轮优先在不包含 Skill 正文及历史原始轨迹的真实维护项目中，保留同等项目治理，重新预注册对照条件并检查间接内容暴露，再验证插件增量作用。Windows、联网检索和自动 reviewer 分别作为独立任务。当前没有足够依据提出产品机制改动，保持现状。

## 收尾检查

- 第一方 Markdown 链接检查沿用 REP-0001 的已审 Python 方法，将来源范围更新为 R-00～R-22，并扫描 profiles/docs/plugins/tests 与根入口；本轮 56 份 Markdown、286 个本地链接，缺失目标 0，12 个 gitlink/checkout/索引 SHA 一致。这里只检查文件目标，不宣称外链或标题锚点全部可用。
- 4 个第一方 Python 文件通过 ast.parse 语法检查；`git diff --check` Passed。插件五个源码文件和 marketplace 与共同基线的 diff 为空。
- 最终测试说明是根据本轮观察整理的新文档，已做命令实跑、链接及独立审查，没有另开模型会话冷读该最终文档。评审指出 development 的旧计划导航和台账验证措辞有歧义，已同步修正，旧实验快照保持原样。

临时匿名评审目录和原始临时输出已在逐文件摘要复核后删除；146 文件原包保留。再次按试用和离线复验的标签查询均无遗留容器，固定镜像仍存在；详见 [final-cleanup.json](evidence/0004/final-cleanup.json)。

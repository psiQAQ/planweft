[简体中文](0012-0.4.0-promotion.md) | [English](0012-0.4.0-promotion.en.md)

# 0.4.0 registry 验收与 promotion

OIDC workflow [34434623009](https://github.com/psiQAQ/planweft/actions/runs/34434623009) 从 `master@f0ca3200f53ce56e0d122dd5241f233648ca9459` 发布 `planweft@0.4.0` 到 npm `next`。workflow 的构建、完整测试、准确归档检查、schema 3 预发布门禁和 provenance publish 均 Passed。

官方 registry 回下载归档的 SHA-256 为 `e611338a619adabb0ba943d75460a01d83e4670dbe7d69f5d1050a1c1467c132`，与预审归档逐字相同；registry SHA-512 integrity 与 SHA-1 也匹配。验收时 `next=0.4.0`，`latest=0.4.0-rc.1`，因此本记录不表示已经 promotion。

## 五宿主远端结果

Codex、Claude、Pi、OpenCode 与 DSH 均从官方 registry 独立下载相同字节，在锁定容器与隔离 profile 中完成安装、doctor、same-version update、内容核对和卸载，五项均 Passed。Pi RPC、OpenCode debug 与 DSH compose/boot 使用新进程完成原生发现；补充的 Codex/Claude 检查在安装和 update 后分别另起无凭据宿主进程执行 native plugin list，均发现唯一正确注册。所有容器按 ownership label 核对后删除，成功运行的 npm cache 与 bootstrap cache 已清理。

这些新进程检查不调用模型，也不证明 hooks 执行或模型上下文送达。五宿主真实模型 Skill 读取仍以预发布阶段限定的一次运行为准，没有追加模型重试。

RC15→stable→RC15→stable 的完整远端往返未重复执行。对应 `candidate_to_release`、`release_to_candidate`、`candidate_to_release_again` 以及五宿主 `native_update` 按发布计划使用最终 stable 归档的确定性生命周期、RC15→stable 逐文件 manifest 和独立复用审查；stable 卸载、原生安装/发现/remove 与 fresh-process loading 使用本轮新证据。本轮另观察到的 same-version update 不冒充跨版本 `native_update` 证据。

## Promotion 边界

附件 `evidence/0010/planweft-0.4.0-registry-acceptance.tar.gz` 保存 94 个脱敏结果/日志，manifest 覆盖全部成员且 SHA-256 全部匹配；隐私扫描未发现个人路径、认证头、私钥或常见 npm/GitHub token。`evidence/0010/planweft-0.4.0-registry-proof.json` 绑定 registry 摘要、dist-tags、源码提交和 publish workflow。

只有独立 promotion review、完整 68 项 attestation、`check-release-gate.py --promotion` 和提交后的 CI 全部 Passed 后，才可将 npm `latest` 指向 `0.4.0`。Git tag 与 GitHub Release 仍须在 promotion 之后创建。

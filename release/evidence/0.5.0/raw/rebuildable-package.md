# 0.5.0 可重建包

日期：2026-09-13。用发布工作流固定的 Node.js 24.20.0 与 npm 11.11.0 执行
`npm pack --pack-destination`，从合并后的 `master` 生成 `planweft@0.5.0`。
归档为 `planweft-0.5.0.tgz`，包含 4,339 个成员；SHA-256 为
`431adcc83c773e57bf8b3828bddf34b8043145adc02e522d8a925eaec6ea6eda`。

该归档由本次 prepublication gate 直接读取其 `package/package.json`，以确认包名和版本。

# 0.5.0 可重建包

日期：2026-09-13。`npm pack --pack-destination` 从合并后的 `master`
生成 `planweft@0.5.0`。归档为 `planweft-0.5.0.tgz`，包含 4,339 个成员；
SHA-256 为 `7104584984ca6c49734fe97c775b10f4edbd0a652f11dbd4e22ef43b89951898`。

该归档由本次 prepublication gate 直接读取其 `package/package.json`，以确认包名和版本。

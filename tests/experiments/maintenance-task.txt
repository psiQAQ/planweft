请修复本仓库 tests/run-plugin-smoke.py 的入口参数校验，并按现有项目约定完成必要的回归测试、使用说明、设计依据和可接续记录。

已确认要求：
- --cases 中未知或重复的场景名，以及 --timeout 为零或负数，必须给出清楚的参数错误并以退出码 2 结束；在创建输出目录及父目录、调用 Docker、访问认证之前拒绝。
- 合法场景为现有 CASES 加 repo-copy；保留默认场景、多个不同合法场景和正整数超时。
- handoff 是 enabled 成功后的内部阶段，不接受为用户指定场景；enabled 失败时不运行 handoff。
- 插件本身保持 0.1.0，不改变其文件或能力，不添加第三方依赖。保留已有修改和历史；验证参数错误时使用标准库 mock 或受控替身，不实际运行 Docker 或模型。

共同材料：已有项目文档和 docs/reference/python-argparse.md。选择最小充分实现，完成后说明实际结果与验证限制。

实验环境边界：只编辑 /workspace 中本任务相关文件，可以读取安装的 Skill；.submodule 是只读研究材料，不执行其工具。不要查看凭据、环境变量、其他宿主目录或 /distribution；不联网研究、不安装软件、不运行真实模型或 Docker、不提交或推送 Git。项目历史保留；README 中已有的本地注释是应保留的未提交内容。本环境没有子 Agent 委派能力，按项目约定如实记录不可执行的独立审查，后续由主 Agent 在实验外安排。仓库中的 Skill 源码是项目文件，不代表已经安装启用。
